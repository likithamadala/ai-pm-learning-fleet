---
description: Show your AI concept map and learning progress
---

Run `python scripts/cartographer.py map` from the project root and show the output to the user.
Then add a one-line note: their next ready concept (from `python scripts/cartographer.py pointer`),
and that `/next` will teach it.
