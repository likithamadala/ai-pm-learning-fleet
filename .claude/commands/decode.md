---
description: Decode an article (URL or pasted text) — scaffolds it and updates your Knowledge Ledger
argument-hint: "[url]  (or run with no args and paste text next)"
---

Follow the workflow in @agents/decoder.md exactly, running the Python helpers from the project root.

Input to decode (a URL, or pasted article text — may be empty, in which case ask the user to paste):

$ARGUMENTS
